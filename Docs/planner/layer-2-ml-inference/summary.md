# Layer 2 - ML Inference Summary
Implemented inference-focused APIs that consume transaction features, emit risk scores, and provide model-lab artifacts (feature stats, accuracy curves, drift baseline) while explicitly avoiding model training.
