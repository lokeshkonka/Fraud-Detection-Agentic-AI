# Graph Service

Placeholder service that will front Neo4j graph queries. Currently returns a tiny mock subgraph.

## Run locally

```bash
pip install -r requirements.txt
uvicorn main:app --reload --port 8900
```
