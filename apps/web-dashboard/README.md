# Web Dashboard

React + Vite + Tailwind scaffold for the fraud detection UI. Includes placeholder Model Ops view and the eight route placeholders from the summary plan.

## Run locally

```bash
npm install
npm run dev -- --host --port 3000
```

### CSS utilities

Tailwind is configured with a dark palette plus accent colors `acid` and `lava` to match the agentic UI concept.
