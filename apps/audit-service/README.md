# Audit Service

Lightweight FastAPI placeholder that will store audit and case actions. Currently returns mock records.

## Run locally

```bash
pip install -r requirements.txt
uvicorn main:app --reload --port 8800
```
